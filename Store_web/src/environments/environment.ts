export const environment = {
  production: false,
  baseUrl: 'http://localhost:8000/api/', // your API endpoint don't forgot to add public/api at the end of it
  imageUrl: 'http://localhost:8000/storage/images/', // your api endpoint don't forgot to add public/storage/image at the end of it
  appName: 'Adam Technology Food App',
  default_country_code: '91',
};

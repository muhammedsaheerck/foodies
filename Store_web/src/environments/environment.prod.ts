export const environment = {
  production: true,
  baseUrl: 'https://your-api.com/public/api/', // your API endpoint don't forgot to add public/api at the end of it
  imageUrl: 'https://your-api.com/public/storage/images/', // your api endpoint don't forgot to add public/storage/image at the end of it
};
